# Skills Index

Use these for multi-step tasks:

- add-api-endpoint: create route + zod validation + docs/tests
- add-ui-screen: new page with shadcn/ui + i18n + states + tests
- add-db-table-rls: add table + RLS policies + indexes + docs
- add-reminder-rule: reminders end-to-end + timezone correctness
- add-calendar-sync: google + ICS flows + docs
- add-prayer-times-service: timings API + caching + fallback
- seed-daily-content: load Day 1–30 content + admin editing plan
- i18n-add-language: extend languages + coverage checks
- seo-performance-check: SEO + perf audit + prioritized fixes
